frappe.ready(function() {
    $('input[data-fieldname="price"]').attr('data-mask','(00) 0000-0000');
    //data-mask-visible="true" placeholder="(00) 0000-0000" autocomplete="off');

    // frappe.web_form.set_property (‘xxx’,‘hidden’, 1);
    // bind events here
})
